<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:layout_margin="2dp"
    android:foreground="?android:attr/selectableItemBackground"
    android:orientation="vertical"
    android:padding="2dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/item_border"
        android:orientation="vertical"
        tools:ignore="UselessParent">

        <ImageView
            android:id="@+id/thumbnail"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_gravity="center"
            android:layout_marginTop="15dp"
            android:layout_marginBottom="15dp"
            android:adjustViewBounds="true"
            android:contentDescription="@string/app_name"
            android:padding="2dp"
            tools:src="@mipmap/ic_launcher" />

        <ImageView
            android:id="@+id/favBtn"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="10dp"
            android:layout_marginEnd="10dp"
            android:background="@drawable/round"
            android:contentDescription="@string/desc_add_to_favorite"
            android:padding="4dp"
            android:src="@drawable/ic_favorite_false" />

        <ImageView
            android:id="@+id/ivLock"
            android:layout_width="@dimen/iv_lock_width"
            android:layout_height="@dimen/iv_lock_width"
            android:layout_alignParentTop="true"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="10dp"
            android:layout_marginEnd="10dp"
            android:adjustViewBounds="true"

            android:background="@drawable/round_t2"
            android:contentDescription="@null"
            android:maxHeight="@dimen/iv_lock_width"
            android:minHeight="@dimen/iv_lock_width"
            android:scaleType="centerCrop"
            android:visibility="gone"
            tools:src="@drawable/ic_lock"
            tools:visibility="visible" />

        <ImageView
            android:id="@+id/shareBtn"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="10dp"
            android:layout_marginTop="10dp"
            android:background="@drawable/round_t2"
            android:contentDescription="@null"
            android:padding="4dp"
            android:src="@drawable/ic_share" />

        <TextView
            android:id="@+id/sticker_name"
            android:layout_width="match_parent"
            android:layout_height="43.0dip"
            android:layout_alignParentBottom="true"
            android:background="@drawable/title_box"
            android:gravity="center"
            android:singleLine="true"
            android:textColor="@drawable/sticker_title_text"
            android:textSize="14sp"
            android:textStyle="bold" />


    </RelativeLayout>

</androidx.cardview.widget.CardView>
